const db = require("../../data/db-config.js");

module.exports = {

    find(id) {
        return db("schemes as sch")
    },

    findById(id) {
        
    },

    findSteps(id) {

    },

    add(scheme) {

    },

    update(changes, id) {

    },

    remove(id) {

    }

};